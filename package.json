{
  "name": "img-proxy",
  "version": "1.0.0",
  "description": "Proxy images that are blocked by referrer policies.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
